package com.example.carServiceServ;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.example.carServiceServ")
public class CarServiceServApplication {

	public static void main(String[] args) {
		SpringApplication.run(CarServiceServApplication.class, args);
	}

}
